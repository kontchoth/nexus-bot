# signal-sheet-service — Cloud Run runtime dependencies
# boto3 is NOT here (this is GCP)
# Keep lean: every extra package increases cold-start time

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (pooled, async)
httpx>=0.27.0

# GCP SDKs
google-cloud-firestore>=2.16.0
google-cloud-storage>=2.16.0
google-auth>=2.29.0

# Signal engine
python-dateutil>=2.8.2

# Screenshot renderer
Pillow>=10.3.0

# Pydantic (FastAPI model validation)
pydantic>=2.7.0
